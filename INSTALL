Build dependencies include Lua 5.1, libtool, automake 1.9, and Fidelis
only knows what else.

   0. Run automake-1.9 because right now the build system is wack

   1. Run ./configure to create config.auto

   2. make

   3. make install (or sudo make install)