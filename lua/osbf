#!/usr/bin/lua5.1

-- simple command-line client

require 'osbf'

local options, args = osbf.util.getopt(arg, osbf.std_opts)

if not options then
  io.stderr:write(args, '\n')
  os.exit(1)
end

osbf.init(options)

osbf.command_line.run(unpack(args))